# トップページ用画像

このフォルダにトップページの先頭で表示する画像を置いてください。

- **ファイル名**: `hero.jpg` で保存してこのフォルダに配置
- **推奨**: 横長（例: 1920×1080px）で、サロン外観・内観・雰囲気がわかる写真
- `.png` / `.webp` を使う場合は `app/page.tsx` の `TOP_HERO_IMAGE` を `/images/top/hero.png` などに変更してください

画像を追加すると、トップページの一番上に自動で表示されます。画像がない間は仮の画像が表示されます。
